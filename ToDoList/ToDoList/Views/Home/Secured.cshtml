@using System.Security.Claims
@using System.Threading
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    var error = TempData["Error"] as string;
    var success = TempData["Success"] as string;
    // Get the claims values
    var username = ViewBag.userEmail;

    var identity = ClaimTypes.Name;

    // get information about the user from the properties of the ClaimsIdentityProxy object

    Console.WriteLine(identity);

}
@if (!string.IsNullOrEmpty(error))
{
    <h3 class="alert-success" style="padding:20px;">@error</h3>
}
else if (!string.IsNullOrEmpty(success))
{
    <h3 class="alert alert-success" style="padding:20px;">@success</h3>
}
<h2 class="text-black-50">My Account Overview</h2>
<br />

Username: @User.Identity.Name
<br />
Email: @ViewBag.userEmail 
<br /><br />
 <a href="/changepassform" class="btn btn-success mr-3">Change password</a>
